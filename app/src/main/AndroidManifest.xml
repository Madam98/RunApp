<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miernicki_bieganie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lab8_beta">
        <activity
            android:name="com.example.miernicki_bieganie.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lab8_beta.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.miernicki_bieganie.DetailActivity"
            android:label="@string/title_item_detail"
            android:parentActivityName="com.example.miernicki_bieganie.MainActivity"
            android:theme="@style/Theme.Lab8_beta.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.miernicki_bieganie.MainActivity" />
        </activity>
    </application>

</manifest>